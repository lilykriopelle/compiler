// Runs. Testing parentheses.  Breaks on line 13 because the "(" precedes the "=".

class A {
	int a, b, c; 
	int[] array; 
	void main (String [] args) {
		 A a = new A(); 
		 c = a.method();
		 if(a==b) this.println("Hello");  
	}
	int method(int a, int b, string s) {
		while (i < 100) {
			(a = (7 + b));
		}
	}
}